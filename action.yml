# action.yml
name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  pylint-input-args:  # id of input
    description: 'input args to run pylint'
    required: true
    default: 'pylint --help'

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.pylint-input-args }}
    